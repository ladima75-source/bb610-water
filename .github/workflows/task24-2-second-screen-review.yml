name: TASK 24.2.8 Justified Editorial Review
on:
  push:
    branches: [review/task24-2-8-justify]
permissions:
  contents: read
jobs:
  browser:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --no-save playwright@1.55.0
      - run: npx playwright install --with-deps chromium
      - name: Serve repository root
        run: python3 -m http.server 4173 --bind 127.0.0.1 >/tmp/task24-2-8-http.log 2>&1 &
      - name: Validate justified editorial text
        run: node .github/scripts/task24-2-second-screen-review.mjs
